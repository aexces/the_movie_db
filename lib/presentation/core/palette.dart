import 'package:flutter/material.dart';

class Palette {
  static const backgroundColor = Color(0xFF050A30);
  static const primaryColor = Colors.purple;
  // static const primaryColor = Color(0xFF714AAC);
}
